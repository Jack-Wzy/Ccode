#define _CRT_SECURE_NO_DEPRECATE
#include <stdio.h>
int main()
{
	int a, b, c, d;
	puts("请输入三个整数：");
	printf("整数1："); 
	scanf("%d", &a);
	printf("整数2："); 
	scanf("%d", &b);
	printf("整数3："); 
	scanf("%d", &c);
	d = a + b + c;
	printf("他们的和是：%d", d);
	return 0;
}
