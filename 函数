#define _CRT_SECURE_NO_DEPRECATE
#include <stdio.h>
int Add(int x, int y)
{
	int z = x + y;
	return z;
}
int main()
{
	int a = 0;
	int b = 0;
	int c = 0;
	printf("请输入两个数：");
	scanf("%d %d", &a, &b);
	c = Add(a, b);
	printf("c=%d\n", c);
	return 0;
}
